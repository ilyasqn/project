apiVersion: v1
kind: Service
metadata:
  name: rabbitmq
  namespace: microservices
  labels:
    app: rabbitmq
spec:
  type: NodePort
  ports:
    - port: 5672
      targetPort: 5672
      nodePort: 30672
      name: amqp
    - port: 15672
      targetPort: 15672
      nodePort: 31672
      name: management
  selector:
    app: rabbitmq
