apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: microservices
  labels:
    app: ai-service
spec:
  type: NodePort
  ports:
    - port: 8004
      targetPort: 8004
      nodePort: 30004
  selector:
    app: ai-service
